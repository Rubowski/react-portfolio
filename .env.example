# Copy to .env.local and fill in. Never commit .env.local.
# In Vercel: Project → Settings → Environment Variables (use the same name for Production).

# Create a free access key at https://web3forms.com — submissions go to the email you register there.
REACT_APP_WEB3FORMS_ACCESS_KEY=
